# ARCHIVED — old TWA (Bubblewrap) Android build

This folder is the **retired** Android approach (a Trusted Web Activity, package `app.slickchart.twa`).
We're shipping the Android app with **Capacitor** instead (package `com.slickchart.app`, native camera +
push), so this is kept only for reference. **Do not publish this.**

- The Play-listing screenshots that were here are now at **`store-assets/`** (repo root).
- The current native build is documented in `CAPACITOR-BUILD.md` and `FIRST-BUILD-CHECKLIST.md`.
- The real `android/` folder is now free for Capacitor's `cap add android` to create.

You can delete this folder anytime once you're sure you don't want the old TWA config.
