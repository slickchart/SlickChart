{
  "name": "slickchart",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SlickChart — practice management app with a live Square link (clients + upcoming appointments).",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
